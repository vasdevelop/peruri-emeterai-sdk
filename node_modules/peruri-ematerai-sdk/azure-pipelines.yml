# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    mv .npmrc .tempnotnpmrc
    npm install
    npm install -g jest
    echo "TOKEN_SALT=${TOKEN_SALT_TEST}" > .env
    npm run test
    # npm version patch -m "update version to 0.0.2-beta"
  displayName: 'npm install and test'
# - task: Npm@1
#   inputs:
#     command: 'publish'
#     verbose: true
#     publishRegistry: 'useFeed'
#     publishFeed: '88933ed7-e203-45eb-9dcc-248340ce7629/53bd3a2e-802f-4b74-b19a-50b3ffa3d08c'
